[build]
  base = "pillar-dashboard"
  command = "corepack enable && pnpm install && pnpm build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
